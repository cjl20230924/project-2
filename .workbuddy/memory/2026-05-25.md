# 2026-05-25 工作记录

## dose_app.py 双峰剂量逐级分配修正

- 旧逻辑用归一化分布 × 全局 frac 拆分，未体现每级粒径归属差异
- 新逻辑：对每级中值粒径用 `frac * lognormal_pdf` 计算两峰权重，按权重分配该级活度，再分别乘剂量系数累加

## dose_app.py 累计剂量表新增删除选中行功能

- 累计剂量区新增「➖ 删除选中行」按钮，调用 `_del_accum_row()`
- 支持多行选中同时删除（倒序 removeRow 避免索引偏移）
- 删除后自动重算累计总剂量，表为空时清空标签
